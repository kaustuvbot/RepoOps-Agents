# Repo `.codex`

This is the repo-level Codex layer.

Use it for:
- shared Codex context
- repo-local automation scripts
- task lifecycle helpers
- project-specific conventions that should travel with the repo

Do not use it for:
- personal credentials
- global Codex preferences
- machine-local history

Those belong in `~/.codex`.
